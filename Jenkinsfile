pipeline {
    agent any
        stages {
            stage ('one') {
                echo "Build one"
            }
            stage ('two') {
                echo "Build two"
            }
        }
}